#include <iostream>
#include <vector>
#include <algorithm>

#define ll long long

using namespace std;

int main()
{
	int tc;
	cin >> tc;
	int t=1;
	while(tc--){
		int n,c;
		cin >>n>>c;
		vector<vector<int>> v;
		int min,max;
		for (int i = 0; i < n; ++i)
		{
			int x,y;
			cin >>x>>y;
			v.push_back({x,y});
		}
	}
	return 0;
}